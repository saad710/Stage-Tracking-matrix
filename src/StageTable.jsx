import React from 'react';

const StageTable = (props) => {
    const {result} = props;
    console.log(result)
    return (
        <div>
            
        </div>
    );
};

export default StageTable;